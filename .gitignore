node_modules
dist
*.local
.DS_Store
.env

# Local Netlify folder
.netlify

# DB local (SQLite) + logs de dev — no se versionan
server/*.db
server/*.db-shm
server/*.db-wal
*.log

# Material de trabajo pesado (videos crudos de prueba) — no se versiona
shorts-nature/
_shots/
